VERSION='0.1'
APPNAME='uniclop'
srcdir = '.'
blddir = 'output'

def set_options(opt):
    print '  setting the options'
def configure(conf):
    print '  executing the configuration'
def build(bld):
    print '  building the project'

